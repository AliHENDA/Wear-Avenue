{% extends 'base.html.twig' %}

{% block title %}{{ name}} - Sapes Avenue{% endblock %}

{% block main %}
<div class="small-container">

            <div class="row row-2">
                <h2>All Products </h2>
                <select name="" id="">
                    <option value="">Defaut Shorting</option>
                    <option value="">Sort by price</option>
                    <option value="">Sort by popularity</option>
                    <option value="">Sort by rating</option>
                    <option value="">Sort by sale</option>
                </select>
            </div>

            <div class="display">
            {% for product in products %}     
                <div class="col-4">
                    <a href="{{ path('app_front_product', {slug:product.slug}) }}"><img src= "{{ asset('assets/img/women-01.jpg') }}" alt=""></a>
                    <h4>{{ product.name }}</h4>
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}
